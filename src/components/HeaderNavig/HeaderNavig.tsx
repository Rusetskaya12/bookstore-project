import { HeaderLink } from "../HeaderLink/HeaderLink";
import { StyledNavig } from "./styles";

export const HeaderNav = () => {
  return (
	  <StyledNavig>
      <HeaderLink />
      <HeaderLink />
      <HeaderLink />
	  </StyledNavig>
  );
};